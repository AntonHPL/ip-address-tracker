import styled from "styled-components";

export const HeaderInfoContainer = styled.section`
    background: linear-gradient(90deg, rgba(2,0,36,1) 0%, rgba(65,65,153,1) 0%, rgba(0,212,255,1) 100%);
    height: 30vh;
    display: flex;
    flex-direction: column;
    align-items: center;
`;

export const HeaderInfo = styled.div`
    height: 20vh;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 16px;
`